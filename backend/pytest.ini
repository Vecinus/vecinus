[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Excluir tests de regresión AI (se ejecutan solo en su propio workflow), para no agotar llamadas a la API
collect_ignore_glob = ["tests/ai_regression/*"]
